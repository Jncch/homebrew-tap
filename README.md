# Jncch's Homebrew Tap

## Usage

```sh
brew install --cask jncch/tap/entruce
```

## Casks

| Cask | Description |
|---|---|
| `entruce` | Menu bar utility that unifies Enter-key behavior (Enter = newline, Cmd+Enter = send) across chat and AI desktop apps on macOS |
